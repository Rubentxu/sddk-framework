name: approval-denied-creates-node
events:
  - event_type: approval.capability.requested
    subjects: [[cycle, c-1], [capability, git.push]]
    cycle_id: c-1
  - event_type: approval.capability.denied
    subjects: [[actor, bob], [capability, git.push]]
    cycle_id: c-1
expect:
  nodes: 3
  edges: 2
