name: proposal-rejected
events:
  - event_type: proposal.submitted
    subjects: [[proposal, p1], [cycle, c-1]]
    cycle_id: c-1
  - event_type: proposal.rejected
    subjects: [[proposal, p1]]
    cycle_id: c-1
expect:
  nodes: 2
  edges: 2
